# ==================== CONFIG TWEAKING NOTES ===========================
    # NOTE: ??? match NUM_STEPS w/ num_inner_steps for episode length
    # NOTE: match GIF_NUM_FRAMES w/ num_inner_steps if want eval to be full episode length
    # NOTE: run # rollouts (NUM_UPDATES) > 10, bc with new chunk refactor, it will still run 10 chunks of size 1 if NUM_UPDATES < 10


# ====== tiny test (# rollouts = 2)
# "NUM_ENVS": 2
# "NUM_STEPS": 8
# "TOTAL_TIMESTEPS": 32
# "UPDATE_EPOCHS": 1
# "NUM_MINIBATCHES": 1

# ====== small test (# rollouts = 8)
# "NUM_ENVS": 4
# "NUM_STEPS": 16
# "TOTAL_TIMESTEPS": 512
# "UPDATE_EPOCHS": 2
# "NUM_MINIBATCHES": 2

# NUM_UPDATES == 1e8 // 100 // 4 = 250,000 ??

"LR": 0.001
"NUM_ENVS": 4
"NUM_STEPS": 16
"TOTAL_TIMESTEPS": 512
"UPDATE_EPOCHS": 2
"NUM_MINIBATCHES": 2
"GAMMA": 0.99
"GAE_LAMBDA": 0.95
"CLIP_EPS": 0.2
"ENT_COEF": 0.01
"VF_COEF": 0.5
"MAX_GRAD_NORM": 0.5
"SCALE_CLIP_EPS": True

"ACTIVATION": "relu"
"ENV_NAME": "clean_up"
"REW_SHAPING_HORIZON": 2.5e6 # for how many timesteps add a shaping reward

"ENV_KWARGS": 
  "num_agents" : 7
  "num_inner_steps" : 16
  "shared_rewards" : True  # Attention: switch to False for individual rewards 
  "cnn" : True
  "jit" : True
  

"ANNEAL_LR": True
"SEED": 30
"NUM_SEEDS": 1
"TUNE": False

"REWARD": "MAPPO" # individual; common
"GIF_NUM_FRAMES": 20

# WandB Params
"ENTITY": "rohit-chawla28"
"PROJECT": "socialjax-ippo"
"WANDB_GROUP": "MAPPO-cleanup"
"WANDB_MODE" : "online"
"WANDB_TAGS":
  - COMMON_REWARD # COMMON_REWARD; INDIVIDUAL_REWARD
